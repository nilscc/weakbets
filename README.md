Install
=======

Before running, please add your RapidAPI Alpha Vantage API key, see the
[AlphaVantage README](weakbets/alphavantage/README.md).

Install all Python requirements using the `requirements.txt` file:

```bash
> pip install -r requirements.txt
```
